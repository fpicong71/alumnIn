@model WebFormacion.Models.EntidadContacto

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>EntidadContacto</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="EntidadID" class="control-label"></label>
                <select asp-for="EntidadID" class ="form-control" asp-items="ViewBag.EntidadID"></select>
            </div>
            <div class="form-group">
                <label asp-for="ContactoID" class="control-label"></label>
                <select asp-for="ContactoID" class ="form-control" asp-items="ViewBag.ContactoID"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
